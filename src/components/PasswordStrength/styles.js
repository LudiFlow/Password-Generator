import styled from "styled-components";

const StyledStrengthBar = styled.section`
  display: flex;
  align-items: center;
  div {
    width: 10px;
    height: 28px;
    border: 1px solid var(--light-grey);
    margin-left: 5px;
  }
`;

export default StyledStrengthBar;
